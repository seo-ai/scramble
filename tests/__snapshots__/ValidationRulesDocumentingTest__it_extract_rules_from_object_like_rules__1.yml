-
    name: channels
    in: query
    schema: { type: object, properties: { agency: { type: object, nullable: true, properties: { id: { type: integer, nullable: true }, name: { type: string, nullable: true } } } } }
